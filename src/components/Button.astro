---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'button'> {
  classes?: string;
  type?: 'button' | 'submit';
  variant?: 'white' | 'orange';
}

const { classes, type, variant = 'white', ...buttonProps } = Astro.props;
---

<button
  class:list={[
    'flex h-11.5 items-center justify-center rounded-md px-2.5 text-base font-semibold transition-all duration-300 ease-in-out',
    variant === 'orange' &&
      'border-warning-500 bg-warning hover:border-warning hover:bg-warning-hover border text-white',
    variant === 'white' &&
      'text-gray-solid border-none bg-white hover:border hover:border-white hover:bg-white/80',
    classes,
  ]}
  type={type ?? 'button'}
  {...buttonProps}
>
  <slot />
</button>

<style></style>
